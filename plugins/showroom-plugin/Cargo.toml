[package]
name = "showroom-plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
shiori-plugin = { workspace = true, features = ["extism"] }
serde = { version = "1", features = ["derive"] }
anyhow.workspace = true

fake_user_agent.workspace = true
url = "2.5.4"
